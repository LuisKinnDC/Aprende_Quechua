<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/color_500"/>
    <corners android:radius="16dp"/>
    <stroke android:width="2dp" android:color="@color/color_700"/>

    <!-- Patrón diagonal opcional -->
    <stroke
        android:width="1dp"
        android:color="@color/color_400"
        android:dashWidth="8dp"
        android:dashGap="4dp"/>
</shape>